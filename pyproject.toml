[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bnd"
version = "0.2"
description = "Behavioural & neural data pipeline CLI"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    # -- CLI (lightweight base) ----------------------------------------------
    "typer>=0.9",
    "rich",
]

[project.optional-dependencies]
processing = [
    # -- data I/O & NWB ------------------------------------------------------
    # These neuroscience packages still ship breaking API changes between minor
    # releases (e.g. spikeinterface dropped `load_sync_channel`), so they are
    # pinned to the known-good minor line.
    "pynwb>=3.1.3,<4",
    "ndx-pose>=0.3.0,<0.4",
    "neuroconv>=0.9.3,<0.10",
    "h5py",

    # -- electrophysiology ---------------------------------------------------
    "spikeinterface>=0.104.7,<0.105",
    "probeinterface>=0.3.2,<0.4",

    # -- scientific stack ----------------------------------------------------
    "numpy",
    "pandas",
    "scipy",

    # -- misc ----------------------------------------------------------------
    "pydantic",
    "python-dateutil",
    "pytz",
]
dev = [
    "pytest",
    "bnd[processing]",
]

[project.scripts]
bnd = "bnd.cli:app"

[project.urls]
Homepage = "https://github.com/AtMostafa/bnd"
Repository = "https://github.com/AtMostafa/bnd"

[tool.hatch.build.targets.wheel]
packages = ["bnd"]
